def create_model(layers, learning_rate, weight_initializer, optimizer, optimizerArgs={}):
    # This is just going to be used as a helper to initialize weights for an entire model instead of per layer.
    pass
